source "https://rubygems.org"

# The GitHub Pages gem locks all dependency versions to match what
# github.com/pages runs in production – the safest choice for a
# ready-to-push Pages site.
gem "github-pages", group: :jekyll_plugins

# Local preview (optional): run `bundle exec jekyll serve` after `bundle install`.
